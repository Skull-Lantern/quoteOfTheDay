from sys import platform

class getOS():
    """docstring for getOS."""
    def __init__(self):
        super(getOS, self).__init__()
        self.arg = arg

    def getOsType():
        if platform == "linux" or platform == "linux2":
            # print("The system is: "+platform)
            return platform
        elif platform == "darwin":
            # print("The system is: "+platform)
            return platform
        elif platform == "win32":
            # print("The system is: "+platform)
            return platform
